import 'package:flutter/material.dart';

import '../../core/ui/tab_placeholder.dart';

/// 마이 탭 — 다음 단계에서 만든다.
class MyScreen extends StatelessWidget {
  const MyScreen({super.key});

  @override
  Widget build(BuildContext context) => const TabPlaceholder(title: '마이');
}
